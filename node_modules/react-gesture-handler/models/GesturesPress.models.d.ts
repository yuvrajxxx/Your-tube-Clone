/// <reference types="hammerjs" />
import { RecognizerBase } from './Gestures.models';
export declare enum PressEvent {
    PRESS = "press",
    PRESS_UP = "pressup"
}
export declare type PressEvents = Partial<Record<PressEvent, HammerListener>>;
export interface RecognizersPress extends RecognizerBase {
    events?: PressEvents;
}
