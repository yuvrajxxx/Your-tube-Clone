/// <reference types="hammerjs" />
import { RecognizerBase } from './Gestures.models';
export declare enum SwipeEvent {
    SWIPE = "swipe",
    SWIPE_LEFT = "swipeleft",
    SWIPE_RIGHT = "swiperight",
    SWIPE_UP = "swipeup",
    SWIPE_DOWN = "swipedown"
}
export declare type SwipeEvents = Partial<Record<SwipeEvent, HammerListener>>;
export interface RecognizersSwipe extends RecognizerBase {
    events?: SwipeEvents;
}
